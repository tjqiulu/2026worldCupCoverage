# 2026-06-16 Plan 019 — 积分榜加 GF/GA/GD 列

## 状态

Plan 019 状态 `planned` → `completed`。Commit `66c3d30`。

## 触发

11:09 用户截图 G 组积分榜，4 队同分，表格只有 MP/W/D/L/PTS 5 列，问我"API 有净胜球数据吗？或者进球失球数据，计算一下也行？"。

## 关键发现

- **API 完全有 GF/GA/GD 字段**（实测 `https://worldcup26.ir/get/groups` Group G 返回 `gf/ga/gd` 全字段）
- 后端 `find_group_standings` 整 dict 透传
- `src/app.py:106` 注入到 `m["standings"]` 时未过滤
- **卡点在前端 `src/static/js/main.js:renderModalStandings` 只渲染 mp/w/d/l/pts**，t.gf/t.ga/t.gd 拿到但没用

## 决策日志

### 1. 改前端而不是补计算逻辑
**问题**: API 已有但 UI 没显示，怎么修？
**候选**:
- (A) 改 `renderModalStandings` 加 3 列（**采用**）
- (B) 后端兜底——根据 `data/details.json` 里的进球列表自己算 GF/GA/GD 后注入
- (C) 改后端 `find_group_standings` 自己做计算逻辑覆盖 API
**决定**: A
**理由**:
- A 改动最小（< 50 行 JS），API 数据是事实源
- B/C 都重复造轮子（API 已经在算了），且会和 API 的 ranking 规则（pts desc, gd desc, gf desc）打架

### 2. GD 加 `+`/`-` 号不加颜色
**问题**: 净胜球列要不要颜色提示？
**候选**:
- (A) 只加文本前缀（`+5` / `0` / `-3`）（**采用**）
- (B) 加颜色（绿色 + 红色）+ 文本
- (C) 啥都不加，只显示数字
**决定**: A
**理由**:
- 第一版先验证数据正确性，颜色等用户反馈再迭代
- 避免一次性把 UX 推得过深（AGENTS.md 风格"不堆砌"）
- GD 列用 `font-weight: 600` 视觉上已能突出

### 3. 不引 JS 测试框架
**问题**: 这个改动要不要加测试？
**决定**: 不加
**理由**:
- 项目 tests/ 全是 Python，**无 JS 测试框架**
- `_gdStr` helper 极简（3 行），静态可读
- 数据流已用 curl 在 plan 根因段实测过
- 改完跑 `pytest -q tests/` 200/200 全过，确认 Python 端没回归

## 改动清单

| 文件 | 改动 | 行数 |
|------|------|------|
| `src/static/js/main.js` | + `_gdStr()` helper，加 3 个 `<td>` + 3 个 `<th>` | +13 |
| `src/static/css/main.css` | + `.standings-gd` 视觉样式 | +6 |
| **`src/app.py`** | **0 改动** |  |
| **`src/data/worldcup_api.py`** | **0 改动** |  |
| **`data/`** | **0 改动** |  |

**总：+19 行**（plan 预估 < 50 行 ✓）

## 验证

### 1. 单元测试
```
200 passed in 45.13s
```

### 2. GD 文本格式化（合成数据）
```
Brazil     gf=  7 ga=  2 gd=  +5
Mexico     gf=  5 ga=  3 gd=  +2
Croatia    gf=  3 ga=  3 gd=   0
Cameroon   gf=  2 ga=  5 gd=  -3
Canada     gf=  0 ga=  4 gd=  -4
```

### 3. G 组实际 API 数据模拟渲染
```
#  球队                      MP   W   D   L  GF  GA   GD  Pts
------------------------------------------------------------
1  Iran                     1   0   1   0   2   2    0    1
2  New Zealand              1   0   1   0   2   2    0    1
3  Belgium                  1   0   1   0   1   1    0    1
4  Egypt                    1   0   1   0   1   1    0    1
```

10 列全部正确填充。GD 都是 0（4 队都是 1-1），所以同分时 GD 也打平时，按 GF 降序排序：Iran/NZ（gf=2）在前，Belgium/Egypt（gf=1）在后——这与 FIFA 标准排序规则一致。

### 4. 负数 GD case 验证
等 6/16 出现非 1-1 比赛后，刷新 modal 看 GD 列显示 `-2` / `+1` 这类带正负号的格式。**当前数据无此 case，留给用户视觉验收**。

## 待办

- [ ] 用户实际看 modal，确认 10 列布局在桌面全屏下不挤
- [ ] 如果要加 GD 颜色 → 起新 plan（不在本 plan scope 内）
- [ ] 如果要加进球/失球场均 → 起新 plan

## Next-Session Pickup Notes

- Plan 019 完结，本 plan 状态 `completed`
- 关联 plan 015 仍 in-progress（详情弹窗其它优化未做）
- 数据/details.json 仍有一份未 commit 改动（用户后台 refresh 进程写的），留用户决定
