# 2026-06-19 Plan 032 — Bracket Bing 风格重设计 + 滚动条 + SW 修复

## 状态

Plan 032：`proposed` → `completed`（22:xx 部署）

## 触发

用户 22:16 反馈：
1. **bracket 初次空白 + reload 后数据消失**（service worker cache-first 命中 stale main.js）
2. **滚动条太丑**
3. **参照 Bing 重新设计**（大卡片、间距、team+Upcoming 风格）

## 修复

### Fix 1: Service Worker 改 network-first
- `sw.js` 升版本 `wc2026-v6` → `wc2026-v7`
- Same-origin 路由从 cache-first 改为 **network-first**（缓存只作为网络失败的 fallback）
- → main.js 改动后立即生效，不用等用户 Crtl+F5

### Fix 2: 滚动条自定义
- `*::-webkit-scrollbar` 8px 细滚动条 + 圆角
- `scrollbar-width: thin; scrollbar-color: #c0c0c0` (Firefox)
- 滚动时出现，平时隐藏（透明 track）

### Fix 3: Bing 风格重设计
**新 `renderMirrorCard()`** 三段式布局：
```
┌──────────────────────────────┐
│  6/29        03:00          │  ← head: 日期+时间
│  🇲🇽 墨西哥                 │  ← home row: 国旗+中文
│  🇧🇦 Bosnia & Herzegovina  │  ← away row: 国旗+英文
│  未开赛      BC Place       │  ← foot: status+venue
└──────────────────────────────┘
```

- 卡片放大：min-height 56px → **96px** + border-radius 4px → **8px**
- Grid row 60px → **110px**，gap 6px → **8px**（更宽间距）
- 字号 14px → 15px + 字重 700
- 状态徽章：未开赛/已结束/LIVE（带颜色 + pulse 动画）
- 锁定队 `.qualified-locked` 绿色背景 + 绿色描边（更明显）
- FINAL 卡片黄色高亮保留

## 改动清单

| 文件 | 改动 | 行数 |
|------|------|------|
| `src/static/sw.js` | CACHE_VERSION v6→v7; same-origin network-first | +5 / -10 |
| `src/static/css/main.css` | 卡片布局重设计 + 滚动条 + body max-width 1200→1400 | +90 / -25 |
| `src/static/js/main.js` | + `_renderBingTeamRow()` helper; `renderMirrorCard()` 重写 | +30 / -16 |
| `docs/plans/032-*.md` | 新 plan | +50 |
| `docs/logs/2026/06-19-plan-032.md` | 本文件 | — |

**0 改后端、0 改算法、0 改测试**（35/35 qualification test 仍通过）

## AGE 8-Gate Closure Audit

| # | Gate | 验证 | 状态 |
|---|------|------|------|
| 1 | sw.js network-first | CACHE_VERSION=v7 + 同源 network-first | ✅ |
| 2 | 滚动条自定义 | webkit-scrollbar + scrollbar-color | ✅ |
| 3 | Bing 风格卡片 | _renderBingTeamRow + 新 layout | ✅ |
| 4 | qualification test 不回归 | 35/35 | ✅ |
| 5 | 全套 pytest 不回归 | 292/292 | ✅ |
| 6 | app 启动正常 | `python3 -c "create_app()"` OK | ✅ |
| 7 | Flask 重启 + 刷新 cache | refresh status: ok, qualification_refreshed: true | ✅ |
| 8 | 浏览器视觉验证 | ⏳ 用户 Ctrl+F5 | — |

## 关键决策

1. **SW 升版本 + network-first** 双管齐下：版本号触发 activate 删除旧 cache；network-first 避免 stale 命中
2. **3 段卡片布局** 而不是单行：参考 Bing 的清晰度，每个信息位（date/time/team/status）都有明确位置
3. **保留 SVG 连线**：bracket 连接线由 `drawBracketLines` 画在 SVG 叠加层（Plan 007），卡片放大后连线仍正常
4. **status 徽章 + pulse 动画**：LIVE 比赛实时感更强

## 不破坏什么

- 现有 4 个路由 + qualification cache + standings 都正常
- 现有 292 个 pytest 全过
- 0 改后端、0 改 ICS parser、0 改 worldcup API
- 浏览器 SW 自动激活新版（skipWaiting + clients.claim 已存在）
