# 2026-06-12 — 项目初始化 + Plan 001 骨架

## 状态

项目从零开始。Plan 001（initial skeleton）执行完，状态 `proposed` → 待用户 closure audit。

## 关键数字

- **16 个文档文件**（10 已写完 + 1 plan 本文件 + 1 log 本文件 + 4 个 context/README + 1 个 docs/index）
- **19 个目录**（1 项目根 + 8 docs/ + 8 src/ + 1 data/ + 1 tests/）
- **0 行代码**
- **0 占位符**（待 closure audit 验证）

## 决策日志

### 1. UI 语言：双语
**用户决定**: 中文 + 英文都要
**影响**: `data/i18n/{zh,en}.json` 双份维护；队名要中英并列

### 2. 数据源：A 方案
**用户决定**: 优先爬 baires/fifa-cal-2026 ICS，补细节
**影响**:
- 后端有 ICS fetcher 模块
- `data/details.json` 手写维护进球/场馆
- ICS 是 derived 数据，可重生成

### 3. 桌面形态：A 方案
**用户决定**: 浏览器全屏（不打包本地 app）
**影响**:
- 用 `chromium-browser --kiosk` 或 `firefox -kiosk`
- `src/start.sh` 一条命令启动 Flask + 全屏浏览器
- 关闭浏览器要同时关闭 Flask

### 4. 比分更新：刷新模式
**用户决定**: 不要实时，刷新即可
**影响**:
- 不连 football-data.org 实时 API
- 不连 WebSocket
- 顶部 "刷新" 按钮触发 `POST /api/refresh`

### 5. 项目路径
**用户决定**: `/home/lqiu/.openclaw/workspace/2026worldCupCoverage/`
**影响**: 标准 OpenClaw workspace 下的项目子目录

## Next-Session Pickup Notes

最重要的文件按这个顺序读：

1. `AGENTS.md` (root)
2. `docs/index.md`
3. `docs/context/project-context.md`
4. `docs/context/ai-autonomy-policy.md`
5. `docs/context/codebase-map.md`
6. `docs/backlog/README.md`（找 active item）
7. `docs/plans/001-initial-skeleton.md`（看 plan 状态）
8. `docs/logs/2026/06-12.md`（本文件）

## 5 种可能场景

1. **Plan 001 还没 close** → 跑 closure audit（grep 占位符 + 验证目录结构 + 检查链接）
2. **Plan 001 closed** → 选下一个 P0：
   - P0-002 ICS parser（推荐先做，解锁其他）
   - P0-003 Flask skeleton
3. **想直接跑 MVP** → 把 P0-002 + P0-003 + P0-004 合并成一个 plan
4. **想改设计** → 改 `docs/design/app-overview.md`（L1）→ 重新出 plan
5. **遇到 blocker** → 看 project-context.md 里的 blocker 段 → 在 log 里记录

## 这次执行的最大收获

1. **AGE 从零开始也能跑通**——image-indexer 项目的 AGE 是从已有项目改造的，本项目是纯新建，证明 AGE 适合任何起点
2. **轻量级 harness 选择**：不复用 image-indexer 的 6 个 context 模板，针对本项目裁剪到 5 个 + 必要的架构/设计/需求 3 个
3. **Flutter 没装**——事先不知道，但 Python+node 都有，验证了技术栈建议是对的
4. **手写 details 是软肋**——MVP 可能只覆盖几场示范比赛，要提前告诉用户预期
